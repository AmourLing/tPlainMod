# 更新说明

## 2026.09.02

### BetterAchievementUnlocker 成就解锁器

- 从"一键解锁全部"重做为逐成就控制: 全部成就图标网格 (原版雪碧图真实图标), 左键选中/取消, 右键立即解锁单个。
- 操作按钮: 全选 / 全不选 / 解锁选中 / 全部解锁; 搜索框 (ID/名称); 「隐藏已完成」过滤。
- 右键已完成成就 = 取消解锁 (本地清除进度, Steam 后端开启时走本地回退)。
- 选中状态持久化 (selectedAchievements.json); 状态栏替代聊天提示。
- 解锁核心重写: 仅枚举条件字典需一次反射, 完成链路走原版公开 API (Condition.Complete → 自动计数/存档), 移除手动改 _completedCount 的风险操作。

### BetterInfoDisplay 更好的信息显示

- 角色信息页重构: 基础 / 移动 / 永久增益分区逐行展示, 带状态着色 (满值绿/无敌帧橙/重力反转黄/永久增益绿灰)。
- 新增「伤害详情」页 (设计参考 ImproveGame PlayerStats): 按近战/远程/魔法/召唤分区, 含伤害、暴击、攻速、穿甲、箭矢/子弹加成、法力回复、消耗减免, 加成绿红着色。
- 新增哨兵当前数量统计 (场上 WipableTurret 弹射物数 / maxTurrets, 满员绿色)。
- 功能开关扩至 5 个: 无限飞行 / 无限魔力 / 生命锁定 / 无限呼吸 / 摔落免疫 (每帧维持, 实时生效)。
- 新增快捷操作: 反转重力 / 清除减益。
- 新增模组设置页 (快捷按钮开关) 与 QuickButton 集成。
- 信息页基础设施抽为 InfoPageBase 公共基类, 逐行缓存刷新。

### 仓库

- 补充 README (模组列表、依赖、构建方式、许可证与致谢)。

## 2026.08.31 (3)

### BetterBuffGet 更好的增益获取

**主界面彻底重做（参考 ImproveGame 无限药水界面）**

- 弃用旧版"文字行 + 右侧方框勾选"列表，改为全部游戏增益的图标网格（运行时枚举 Lang.GetBuffName，自动适配版本），左键启用/禁用、右键收藏置顶，悬停显示名称、ID、是否减益（红字）、当前状态、最长持续时间与操作说明。
- 已启用格子绿色底+图标全亮，未启用暗色底+图标压暗，收藏项金色星标置顶。
- 新增搜索框（数字按 ID 前缀匹配 / 文字按本地化名称模糊匹配，标题实时显示「已选 N / 筛选 x/y」）。
- 新增两个过滤开关：「仅背包」（背包内已有物品对应的增益）、「仅可获」（药水/食物/增益站，后者含篝火、红心灯笼、向日葵、和平蜡烛、水蜡烛、猫雕像、旗帜等静态增益站）。

**预设方案**

- 新增命名预设（PresetSetting）：把当前已选增益存为命名方案，支持一键应用、覆盖同名方案、游戏内直接增删改（面板「方案名称」输入框 + 「存为预设」按钮，列表左键应用、右键删除）。
- 主面板「预设方案」区自动列出全部预设并支持即时应用。

**增益逻辑优化**

- 增益时长最低 2 分钟（`max(物品原时长, 7200帧)`），防止短时增益消失过快。
- 自动持续获得改为智能补续：每 10 秒检查一次，仅当增益不存在或剩余 <12 秒时才补续（直接写 buffTime，不走 AddBuff，避免重复触发药水音效/副作用）。
- 新增按键绑定「开关功能」（可配置），功能总开关可独立于自动开关切换。
- 增益时长数据库分帧扫描，完成前按键不生效，避免一次按键扫描全部物品造成卡顿。
- 按 B 上增益时，打开聊天输入框不再误触。
- 所有操作（应用、补续、预设保存、开关切换）不再向聊天框吐消息，界面清爽。

**Bug 修复**

- 修复收藏星标加载失败（`Images/UI/FavoriteStar` 资源不存在），改用游戏自带光标星标图案 `TextureAssets.Cursors[3]`。
- 修复三个过滤开关状态文字越界（UIText 100% 宽度使 HAlign 失效）与背包过滤数据累积的问题，开关改为自绘 ToggleCell（标签左对齐、关/开 右对齐）。

**工程**

- .gitignore 增加 `.user` 忽略（csproj.user 属 IDE 本地配置）。
- 数据文件升级：`selectedBuffs.json` 增加 favorite 字段；新增 `presets.json`（预设方案）；自动持续获得开关为会话内开关（重启重置为关）。

## 2026.08.31 (2)

### BetterPrefix 更好的前缀

**前缀编辑器（主界面）重做**

- 顶部物品区改为两行：物品名 + 「当前前缀: xxx」，获得完美前缀时物品名变红、前缀金色高亮。
- 预设前缀区改为可滚动的按钮墙（ScrollViewer + WrapPanel），支持窗口拖拽缩放后自动换行；预设按钮改为紫色系，与操作按钮区分。
- 底部操作区新增状态栏，每次操作后反馈结果（如「完美重铸: 第 37 次获得完美 神级」）。
- 修复：按钮冷却期间手动设置 BackgroundColor 会被 UIButton1 每帧覆盖，禁用效果从未生效；改用控件自带 isEnable，冷却灰显正常工作。

**重铸功能拆分**

- 随机重铸：改为只随机重铸一次，立即出结果。
- 完美重铸（新增）：反复重铸直到获得完美前缀或达到最大尝试次数，成功后按钮冷却 60 帧并显示尝试次数。
- 预设前缀：保留多次尝试逻辑（单次命中目标前缀概率过低），失败时状态栏提示「未在 N 次内获得 [xxx]，已保留原前缀」，不再静默失败。

**设置界面改版**

- 预设前缀页：新增全前缀总表（从 ID 1 起网格排列，运行时枚举 Lang.prefix，自动适配游戏版本），左键点击格子直接添加/移除预设，绿色 = 已启用、红色 = 未启用、金色 = 搜索匹配。
- 已启用前缀单独以绿色标签列出（「已启用 (N)」区块），点击标签自动定位到表格中对应格子。
- 搜索框支持双模式：数字按 ID 前缀匹配，文字按本地化名称模糊搜索（跟随游戏语言），匹配项标金并自动滚动到第一个结果。
- 最大尝试次数页：弃用自制滑块，改用 tContentPatch 标准 UIItemValueSlider，与其他模组设置风格一致。

**Bug 修复**

- 修复设置页滚动条完全失效的问题：UIList 的滚动范围取自子容器外框高度，UpdateContainer_Height() 在首帧布局前调用会算错且永不修正，现改为每帧刷新（UIList/UIScrollbar 反编译分析结论）。
- 修复三个操作按钮（重置/随机/完美）全部叠在最左侧的问题：补上 HAlign 左/中/右对齐。

### 仓库工程

- 新增 Directory.Build.props，统一配置 dll 引用目录（`$(dllDir)`）与模组输出目录（`$(modDir)`），各模组 csproj 不再硬编码绝对路径（GOG/Steam 目录差异、旧版 Terraria_v1.4.5.6 引用清理）。
- 各模组 info.json 补充 jumpPath 仓库地址。
